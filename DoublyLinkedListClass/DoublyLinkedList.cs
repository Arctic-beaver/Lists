using System;

namespace DoublyLinkedListClass
{
    public class DoublyLinkedList
    {

        /*
        public void Reverse()
        {
            Node right = _tail;
            Node left = _head;

            if (left == null || right == left) return;
            Node tmp = _head;
            _head = _tail;
            _tail = tmp;

            //тут можно круто с Double


            while (right != left)
            {

            }
        }*/
    }
}
